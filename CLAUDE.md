# Selvagen.GH — Grasshopper Plugin

This is a Grasshopper plugin project producing `.gha` assemblies.

**Always invoke the `creating-grasshopper-plugin` skill for any code task in this repo** — editing components, changing icons, building, deploying, scaffolding, or debugging. The skill contains critical guidance on deploy paths, icon design rules, the edit cycle, and common pitfalls that are not obvious from the code alone.
